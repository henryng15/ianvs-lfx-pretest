Reviewed at head `ef4d9ec` against `37a9c60`. The change is correct — but the PR asserts
the removed branch is dead without showing it, and "removes a defensive fallback" is not
something a reviewer should accept on assertion. Here is the verification.

The PR replaces

```python
source = self.dataset.test_url if hasattr(self.dataset, 'test_url') else self.dataset.test_data_info
self.inference_dataset = self.dataset.load_data(source, "inference")
```

with a direct `self.dataset.test_url`. The question is whether `hasattr` can ever be
`False`.

It cannot. `core/testenvmanager/dataset/dataset.py:46` sets

```python
self.test_url: str = ""
```

unconditionally in `__init__`, so the attribute exists on every `Dataset` instance before
any config is parsed. Lines 168-175 then assign it from `test_index`, `test_data` or
`test_data_info`, or raise `NotImplementedError` if none is present — so the object is
never handed to `JointInference` in a state where `test_url` is absent.

One further check, because it is the case that would bite: the original used `hasattr`,
not truthiness. An empty `test_url` (`""`) therefore took the `test_url` branch before
this PR and takes it after. The behaviour is identical, not merely equivalent in the
common case.

The import reordering and blank-line changes in the same diff are unrelated to the
described fix. They are harmless, but splitting them out would make the behavioural part
of this PR reviewable on its own.

**Recommendation: accepted as it is.**
