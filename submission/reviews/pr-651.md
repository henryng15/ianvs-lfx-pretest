Reviewed at head `793718f` against `37a9c60`.

The Python is correct: `__all__ = ('accuracy')` is the string `"accuracy"`, not a tuple,
and `from module import *` would walk it character by character. The 18 modules are worth
tidying and I would merge this.

One correction to the rationale, because it affects how the PR should be labelled.

**Nothing star-imports these modules, and Ianvs never reads `__all__`.** Checked at
`37a9c60`:

```text
star-imports in core/                          : 0
references to __all__ anywhere in core/        : NONE
Example modules with a malformed __all__       : 30
  of those, ever consumed by `from X import *` : 0
```

Core resolves metric and algorithm modules by ClassFactory alias, not by `__all__`:

```python
# core/testcasecontroller/metrics/metrics.py:172-175
load_module(url)
metric_func = ClassFactory.get_cls(type_name=ClassType.GENERAL, t_cls_name=name)
```

`core/testcasecontroller/algorithm/module/module.py:116` and `:130` do the same. There is
no code path on this commit through which the described failure can occur.

So the described bug is real Python but not a reachable Ianvs defect, and this PR changes
no runtime behaviour. That is worth knowing for two reasons: it means the change carries
**zero regression risk** (a genuine argument for merging it), and it means the PR is
`kind/cleanup` rather than `kind/bug`, which currently affects how it is prioritised
against PRs that do change behaviour.

Reproduce: `python3 tools/probe_all_is_inert.py ianvs` —
https://github.com/henryng15/ianvs-lfx-pretest

**Recommendation: minor revision** — no code change needed; re-label and adjust the
description so the next reader is not looking for a runtime symptom that cannot appear.
